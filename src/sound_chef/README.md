# Sound Chef

Sound Chef is the low level library that handles audio playback. It is an extension of miniaudio that tries to move the API closer to FMOD and make it easier for Sound Bakery to interact with. Instead of Sound Bakery handling nodes, it only handles sc_sound objects.